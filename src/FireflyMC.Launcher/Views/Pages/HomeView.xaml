<UserControl x:Class="FireflyMC.Launcher.Views.Pages.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="主页" FontSize="28" FontWeight="SemiBold" Foreground="#111827" />
        <GroupBox Grid.Row="1" Header="启动" Margin="0,24,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ComboBox ItemsSource="{Binding Accounts}"
                          SelectedItem="{Binding SelectedAccount}"
                          DisplayMemberPath="Username"
                          MinWidth="260"
                          HorizontalAlignment="Left" />
                <TextBlock Grid.Row="1" Text="{Binding Status}" Margin="4,10,4,0" Foreground="#4B5563" />
                <Button Grid.Row="2"
                        Content="{Binding MainButtonText}"
                        Command="{Binding MainActionCommand}"
                        Width="180"
                        Height="42"
                        HorizontalAlignment="Left"
                        Margin="4,16,4,4" />
                <StackPanel Grid.Row="3" Margin="4,12,4,4">
                    <ProgressBar Height="10" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" />
                    <TextBlock Text="{Binding ProgressText}" Margin="0,8,0,0" Foreground="#6B7280" />
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
